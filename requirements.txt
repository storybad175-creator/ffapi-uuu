# Runtime
aiohttp>=3.9.0
pycryptodome>=3.20.0
protobuf>=4.25.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# CLI (stdlib argparse used — no extra dependency needed)

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0        # for FastAPI TestClient
freezegun>=1.4.0     # for time-based cache tests
